import { Container } from 'react-bootstrap';

function Chat(): JSX.Element {
  return (
		<Container
  style={{
			  textAlign: 'center',
  }}
		>
			CHAT!!!
		</Container>
  );
}
export default Chat;
