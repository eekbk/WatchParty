import { Form, Col } from 'react-bootstrap';
import styled from 'styled-components';

export const StyledVoiceControl = styled(Form)`
  width: 100%;
`;

export const StyledTranscript = styled(Col)`
  overflow: auto;
`;
